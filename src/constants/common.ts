export const PREFIX = 'Forstand'

export const COOKIE_ITEM = Object.freeze({
  USER_TOKEN: `${PREFIX}_user_token`,
  ADMIN_TOKEN: `${PREFIX}_admin_access_token`,
})

export const STORAGE_ITEM = Object.freeze({
  ACCESS_TOKEN: `${PREFIX}_access_token`,
  ADMIN_USER_INFO: `${PREFIX}_admin_user_info`,
  END_USER_INFO: `${PREFIX}_end_user_info`,
})
